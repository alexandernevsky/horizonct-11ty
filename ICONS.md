# Lucide Icons для проекта

## 📦 Установлено

- ✅ **Lucide** установлен как зависимость
- ✅ Создан shortcode `{% icon %}` для использования иконок в Nunjucks

## 🎨 Использование

### Базовое использование

```nunjucks
{% icon "chevron-down" %}
```

### С параметрами

```nunjucks
{% icon "chevron-down", { size: 20, strokeWidth: 2, class: "w-5 h-5 text-primary" } %}
```

### Доступные иконки

Сейчас доступны следующие иконки (можно расширить):

- `chevron-down` - стрелка вниз
- `chevron-up` - стрелка вверх  
- `check` - галочка
- `sun` - солнце (для light mode)
- `moon` - луна (для dark mode)
- `menu` - меню (гамбургер)
- `x` - крестик (закрыть)

## 📝 Примеры

### В navbar (language switcher)

```nunjucks
{% icon "chevron-down", { size: 16, class: "h-4 w-4 opacity-50 transition-transform duration-200" } %}
```

### В кнопке темы

```nunjucks
{% icon "sun", { size: 20, class: "w-5 h-5" } %}
{% icon "moon", { size: 20, class: "w-5 h-5" } %}
```

### В мобильном меню

```nunjucks
{% icon "menu", { size: 24, class: "w-6 h-6" } %}
{% icon "x", { size: 24, class: "w-6 h-6" } %}
```

## 🔧 Расширение набора иконок

Чтобы добавить новые иконки, отредактируйте `.eleventy.js` и добавьте SVG path в объект `icons` в функции `eleventyConfig.addShortcode("icon", ...)`.

Или используйте готовые SVG из [Lucide Icons](https://lucide.dev/icons/) - просто скопируйте path из SVG иконки.

## 💡 Примечание

Lucide Icons - это библиотека для React, но для статического сайта мы используем готовые SVG paths. Это позволяет использовать те же иконки, что и в shadcn/ui примерах, но без зависимости от React.

